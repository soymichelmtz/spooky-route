#############################################
# Spooky Route - Archivo de ejemplo (.env)  #
# Copiar como .env y reemplazar valores      #
#############################################

# Base de datos (SQLite para desarrollo). Para producción considerar Postgres.
DATABASE_URL="file:./dev.db"

# Secreto para firmar JWT (usa un valor largo y aleatorio, NO este ejemplo)
# Generar (PowerShell): node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="REEMPLAZAR_CON_UN_SECRETO_LARGO"

# Puerto de escucha local. En plataformas (Render/Railway) se usa process.env.PORT.
PORT=3001
